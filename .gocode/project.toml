format_version = 1
goal = "Describe the project goal here"
authorized_root = '\\?\C:\Users\TI\Documents\Projetos\Scripts\gocode'
autonomy_mode = "self-ceo"
operating_intent = "enterprise"
guard_mode = "active"
community_skills = []

[narrative]
enabled = false
intensity = "low"

[notifications]
enabled = true

[team]
agents = []

[technology_rules]
allowed_languages = ["Rust"]
prohibited_tools = [
    "docker",
    "kubernetes",
]

[resource_limits]
max_cpu_percent = 70
max_concurrent_agents = 1
max_processes = 4
max_memory_mb = 4096
max_storage_mb = 1024

[deletion_policy]
max_files = 100
max_bytes = 104857600
material_file_threshold = 1
material_byte_threshold = 1

[skills]
global_project = []
recommended = [
    "safe-workspace",
    "git-worktree-flow",
    "task-planning",
    "code-review",
    "test-first",
    "documentation",
    "resource-aware-execution",
    "nim-resilience",
    "office-drama",
]
granted_permissions = [
    "read-project",
    "write-project",
    "provider-request",
]

[context_lifecycle]
default_provider_cost_class = "paid"
free_or_limited_compact_at_percent = 75

[scope_divergence_policy]
max_unplanned_changed_files = 2

[correction_loop_limits]
max_attempts = 3
max_elapsed_seconds = 14400
max_tokens = 200000

[correction_loop_limits.max_resource_estimate]
cpu_percent = 80
memory_mb = 4096
storage_mb = 4096
processes = 8
agents = 4
